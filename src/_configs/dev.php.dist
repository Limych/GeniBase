<?php

// Enable the debug mode
$app['debug'] = true;

// === Profiler ===============================================================

$app['profiler.cache_dir']  = "${root_dir}/tmp/cache/profiler";

// === Logging ================================================================

$app['monolog.level']   = \Monolog\Logger::DEBUG;

// === Rate Limiter ===========================================================

$app['rate_limiter.autocheck'] = false;
