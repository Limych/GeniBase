{#<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#dlgLogin">
  Launch demo modal
</button>{##}

<div class="modal fade" id="{{ login_id }}" tabindex="-1" role="dialog" aria-labelledby="{{ login_id }}Label" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="{{ login_id }}Label">Войдите в систему</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                {% include 'dashboard/block_login.html.twig' %}
            </div>
        </div>
    </div>
</div>
